# QQ 实际截图识别验证

用户提供空白账号密码登录窗口截图，640×920。使用最新一张：账号和密码提示文字可见，自动登录、记住密码、协议均未勾选。截图无可见个人账号或密码。

测试只发送本地页面预览的图片到 Google Gemini 3.6 Flash，验证页面分类及三个控件的位置，不执行输入、点击登录或同意协议。登录按钮呈禁用样式，识别到位置不代表允许点击。

人工标注见 `input/fixtures.json`。评分使用与虚构样本相同的规则：预测中心位于真实控件内，交并比至少 0.5。

## 第一轮

`run-01/report.json`：1 次请求，约 5.01 秒后发生网络连接或等待错误，未收到模型响应。原版本合并处理了这两类异常，没有记录具体传输原因，因此不能确认是哪一种，更不能将其计为定位失败。

## 第二轮结果

保持图片、模型、提示词、坐标及判定标准不变。新增传输错误分类与详情，网络异常后等待 15 秒最多重试一次；不切换模型或服务。

`run-02/report.json`：1 次请求，9.84 秒后 Google 返回 HTTP 503，明确说明该模型当前需求过高，建议稍后重试。未返回识别文本或坐标。本轮不是传输异常，未触发仅针对传输错误的自动重试。

这说明本次请求已到达 Google 并得到服务繁忙响应，不是用户漏填密钥或截图定位判定失败；也没有证据表明免费额度已用尽。暂不反复要求用户重输密钥，保留截图与测试条件，后续服务恢复后再补测。

截至目前仍未验证真实 QQ 截图识别成功，也未验证本机辅助功能定位或真实输入执行。
