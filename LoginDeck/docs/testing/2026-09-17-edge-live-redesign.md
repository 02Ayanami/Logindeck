# 新版 Edge 真实保存验证

- 使用真实 Edge、已安装的解压扩展、新编译的开发版桌面与 native host；未打包安装器。
- 插件检查连接显示“已连接 LoginDeck”。更新扩展文件后必须重新加载扩展，避免旧后台继续运行。
- 在 https://the-internet.herokuapp.com/login 提交虚构测试账号 `logindeck-integration-0917`。此账号无法登录测试网站；本次验证提交捕获与本地保存，不代表登录成功识别。
- 用户明确确认测试保存后，点击网页中的 LoginDeck 保存按钮。
- 真实插件返回“已保存到 LoginDeck”，操作结果为“the-internet.herokuapp.com · 已新增账号 1”。
- 只读核对真实桌面数据库：对应网站与登录标识符仅一条记录，名称“账号 1”，来源 browser_extension，存在凭据引用。未读取密码内容。
- 页面导航会更换控件定位；最终在导航后读到真实成功提示。
- 测试账号保留在本地。尚未验收：真实密码更新及备注保留、桌面界面同步、断连与超时完整流程。数据库中的凭据引用和服务成功结果不等价于独立的钥匙串读取验证。

## 真实更新测试

- 用户确认后，对同一虚构登录账号提交不同测试密码，并在真实网页保存提示中确认。
- 插件显示“the-internet.herokuapp.com · 已更新账号 1的密码”。
- 与保存前的只读基线比较：对应记录仍只有一条，账号 ID 相同，名称保持“账号 1”，更新时间和凭据引用均已变化。没有读取密码明文。
- 真实新增与更新路径均已通过；自定义备注保留由核心自动测试覆盖，本轮真实记录保留的是默认名称。桌面界面同步、断连与超时完整流程仍未作为真实端到端验收完成。

## 取消、超时与重连

- 提交虚构账号 logindeck-cancel-0917，点击“暂不保存”；数据库对应记录为 0。
- 提交虚构账号 logindeck-expire-0917，看到保存提示，等待 16 秒后提示消失；数据库对应记录为 0。
- 终止本次启动的开发版进程后，提交 logindeck-disconnected-0917，不出现保存提示，数据库对应记录为 0。
- 重启同一开发版后，真实 Edge 插件显示“已连接 LoginDeck”。已恢复开发版运行。
- 桌面原生 UI 验证尚未完成：电脑操作工具无法选择 target/debug/autologin-desktop，返回 Invalid app；应用目录仅列出未运行的已安装版本。没有用旧安装版替代当前开发版验收。桌面同步、复制与应用唤醒仍需当前开发版 UI 验证。
