# LoginDeck 发布前收尾记录

日期：2026-09-17。当前阶段为开发收尾，本轮未打包、签名或发布。

## 当前范围

网站与应用账号管理、复制账号密码、打开网站／唤醒应用，以及 Edge 插件经用户确认后保存账号密码。自动登录、账号切换、短信登录、自动填写和录制器均不在当前产品范围。

检查了前端路由、页面入口、IPC 客户端和桌面命令注册：旧自动化功能已无产品入口。本轮无需重复移除。历史适配器、录制器和底层实验代码保留用于追溯，不作为当前功能；其 README 已增加历史范围提示。

## 本轮修改

- 重写根 README，删除对旧自动登录功能、设置页、识别开关和旧安装路径的介绍。
- 更新中英文 Edge 使用说明，打开插件入口即可看到教程，第二步提供实际目录。
- 产品设计文档补充最新验收状态，明确覆盖早期“实机验证待完成”的阶段记录。
- 前端项目版本对齐桌面版本 0.1.0；插件项目版本对齐已生成清单的 0.2.0。插件构建改为读取 package.json 的版本，不再重复硬编码。未提升实际产品版本。
- 忽略 macOS .DS_Store 和本地 .pnpm-store 缓存，不删除用户工作文件。

## 验证与验收来源

- 既有自动验证：前端 57 项测试通过；连接服务测试通过；TypeScript/Vite 构建通过。此处记录上一轮结果，并非本轮重新执行。
- 既有真实 Edge 捕获测试见 [实机记录](2026-09-17-edge-live-redesign.md)；已修复问题见 [审阅记录](2026-09-17-review-fixes.md)。
- 用户在本轮对话中确认连接状态功能“已经测试了，可以用”，并确认安装教程与实际操作“一致”。记为用户验收通过；不推断额外未说明的测试场景。
- 本轮执行未打包插件构建，成功；检查生成清单与插件项目版本一致，前端与桌面配置版本一致；检查正式桌面命令未注册旧自动化入口。未重新运行已验收的登录捕获流程。

## 后续界限

当前保留开发版本。安装包生成、签名、公证和分发未执行，也未据本轮验收宣称已完成发行安装包验证。开发预览使用模拟数据，预览连接灯不用于证明真实连接。

## 后续授权：本地安装包

用户随后授权构建本地安装包。已完成 release 构建，生成 Apple Silicon 版本 `target/release/bundle/macos/LoginDeck.app` 和 `target/release/bundle/dmg/LoginDeck_0.1.0_aarch64.dmg`。包内插件版本为 0.2.0，含完整插件脚本与本机连接组件。

初始 Tauri 包签名校验失败；已为包内连接组件与应用补充 ad-hoc 本地签名，重新生成带 Applications 快捷方式的 DMG。`codesign --verify --deep --strict` 与 `hdiutil verify` 通过。未使用 Developer ID、未公证、未对外发布。

检查时开发桌面进程仍在运行，未强制退出或替换其文件。安装包启动、已有数据保留与包内插件连接仍需在退出开发版后验收，本次构建检查不替代这些测试。

## 2026-09-18 最终复验

- 前端 13 个测试文件、58 项测试全部通过；TypeScript 与 Vite 生产构建通过。
- Edge 插件 6 项测试全部通过，未打包插件构建成功，生成清单版本为 0.2.0。
- `git diff --check` 通过；未发现已跟踪的构建产物、Python 字节码或 `.DS_Store`。
- 补充忽略 `__pycache__/` 与 `*.py[cod]`，避免本地 Python 检查产生的缓存进入提交。
- 已安装并使用 Rust 1.89.0 执行 `cargo fmt --all -- --check` 和 `cargo test --workspace --locked`；格式检查通过，全工作区测试零失败。测试中有两项按设计忽略：由父测试调用的子进程锁探针，以及需在沙箱外显式运行的临时钥匙串交互测试。
- 修复桌面启动时无条件重新开启浏览器捕获的问题；现在启用与停用状态都会跨重启保留，并增加后端回归测试。
- 维护状态契约仅接受 macOS 登录钥匙串后端。
- Edge 安装教程使用 macOS 文件夹选择快捷键说明。
- 2026-09-18 按产品拆分要求移除本仓库中的其他平台实现、依赖、脚本、文档与专属图标资产；本仓库只维护 macOS 版本。
